# TODO

- 마지막 갱신: 2026-08-16 (Asia/Seoul)
- 현재 단계: Android 비공개 테스트 업데이트 및 출시 준비

이 문서는 아직 해야 할 작업만 관리한다. 완료한 작업의 상세 이력은 `../DEVELOPMENT_STATUS.md`에 기록한다.

- `[ ]`: 대기 중
- `🚧`: 현재 진행 중인 항목의 문장 앞에 표시
- 완료: 검증 결과를 관련 문서에 기록한 뒤 이 TODO에서는 제거

## 지금 진행할 작업

- [ ] 사용자가 `npx supabase db push`와 `npx supabase functions deploy ic-card-report`, `npx supabase functions deploy issue-report-admin`을 실행하고 새 역 교정·한글 역명 제안·버스 회사·거래 유형 제보, 악성 제보 차단과 월간 1년 보관 정리를 실데이터로 확인한다.
- [ ] 사용자가 최신 코드의 빌드 번호를 이전 Play Console 버전 코드보다 높게 설정한다.
- [ ] 사용자가 `flutter build appbundle --release`로 최신 AAB를 생성한다.
- [ ] 기존 비공개 테스트 `Alpha` 트랙에 최신 AAB와 출시 메모를 업로드한다.
- [ ] Play Console 자동 검사와 앱 검토 결과를 확인하고 차단 오류가 있으면 해결한다.
- [ ] 기존 비공개 테스터가 Play Store에서 최신 버전으로 업데이트할 수 있는지 확인한다.

## 교통패스 비교 기능

- 🚧 사용자가 ODPT 토큰을 개발 환경에 설정하고 정적 가져오기 도구로 Tokyo Metro·도에이 실제 JSON을 생성한다.
- [ ] 생성 결과의 역 수·운임 수·한국어 표기와 `시부야` 초성 검색을 브라우저에서 확인한다.
- [ ] 실제 JSON에서 2026년 운임, 역 순서, 환승 표현과 특정 라이선스 조건을 검증한다.
- [ ] ODPT 무료 MVP를 선택한다면 1차 범위를 Tokyo Subway Ticket과 지하철 내부 이동으로 제한할지 결정한다.
- [ ] 도쿠나이 패스용 JR 동일본 운임 데이터의 장기 상용 이용 가능한 출처를 별도로 확보한다.
- [ ] Tokyo Subway Ticket과 도쿠나이 패스의 적용 구간을 기계 판정 가능한 규칙으로 정의한다.
- [ ] API 평가판으로 역 검색·경로 후보·운임 구성요소·사업자·노선 응답을 검증한다.
- [ ] 검증 결과를 바탕으로 1차 기능 범위와 데이터 제공자를 확정한다.
- [ ] Metro↔도에이 연결 운임과 경로 후보를 구현해 현재 동일 사업자 ODPT 자동 운임 범위를 확장한다.
- [ ] 대표 경로 20건 이상을 공식 검색 결과와 대조한 뒤 본 Android 앱 통합 여부를 결정한다.

세부 분석은 [`PASS_COMPARISON_ANALYSIS.md`](PASS_COMPARISON_ANALYSIS.md)를 따른다. 기존 Android 비공개 테스트와 출시 준비 우선순위는 유지한다.

## 비공개 테스트 확인 항목

- [ ] 실제 `03-C5-01 → 03-C5-17` 기록이 `郡中港 → 松山市`, `伊予鉄道 · 郡中線`으로 표시되고 松山市가 高浜線으로 고정되지 않는지 확인한다.
- [ ] 실제 `21/02` 기록이 `역 단말에서 충전`으로 표시되고 `AC-38`을 확인되지 않은 충전 장소로 노출하지 않는지 확인한다.
- [ ] 실제 이요테츠 高浜線 기록 두 건이 `古町 → 梅津寺`, `梅津寺 → 松山市`와 `伊予鉄道 · 高浜線`으로 표시되는지 확인한다.
- [ ] 실제 `region=3 + 05/0D + 0F3D` 기록이 `버스·노면전차 이용`과 `伊予鉄道`으로 표시되고 특정 교통수단·노선·정류장을 추정하지 않는지 확인한다.
- [ ] `npx supabase functions deploy ic-card-report`로 `GATE_WINDOW_PROCESSING` 제보 유형 허용 변경을 배포한다.

## 프로덕션 출시 전

- [ ] 해당 계정에 비공개 테스트 요건이 적용되는 경우 최소 테스터 수와 연속 참여 기간을 Play Console에서 충족한다.
- [ ] 출시 전 보고서의 안정성·성능·접근성 경고를 검토한다.
- [ ] 데이터 보안, 개인정보처리방침, 앱 콘텐츠 선언이 실제 앱 동작과 일치하는지 다시 확인한다.
- [ ] 비공개 테스트 피드백과 수정 내역을 정리한다.
- [ ] 프로덕션 액세스를 신청하고 승인 후 최종 프로덕션 출시 체크리스트를 수행한다.

## 실제 fixture 확보 후 진행

- [ ] `23/13` 신칸센 이용 기록을 확보한 뒤 별도 유형과 금액 의미를 검증한다.
- [ ] `08/03` 승차권 구매 기록을 확보한 뒤 기존 물품 구매와 분리할지 결정한다.
- [ ] 남은 `4A`·`4B`·`CB` 기록을 확보한 뒤 취소·입장 물품 구매 유형을 결정한다.
- [ ] 정산기, 환승 정산기, 창구 단말의 실제 기록을 확보한 뒤 정산·환불 분류를 검토한다.

## Android 출시 이후

- [ ] Apple Developer 계정을 준비한 뒤 iOS NFC capability와 서명을 설정한다.
- [ ] 동일 카드로 iOS 원시 fixture를 확보하고 Android 결과와 비교한다.
- [ ] iOS 실기기에서 NFC 읽기·오류 처리·UI 안전 영역을 검증한다.
