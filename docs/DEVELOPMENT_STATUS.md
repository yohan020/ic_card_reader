# Development status

- 마지막 갱신: 2026-08-14 (Asia/Seoul)
- 현재 Phase: Android 비공개 테스트 업데이트 · 우선순위 7
- 전체 첫 출시 진행률: 비공개 테스트 진행 중
- Phase 0 진행률: 100% (자료 분석과 문서 초안)
- Phase 1 Android 진행률: 100%
- Phase 2 진행률: 100% (파서·역명 조회·거래 분류·금액·실물 카드·익명 제보 검증 완료, 영구 저장은 제품 범위에서 제외)
- 실기기 검증: Android 카드 읽기와 Supabase 익명 제보 확인, iOS는 Apple Developer 계정 준비 전 보류

## 완료: 한국어 역명 보조 표기

- Wikidata CC0 구조화 데이터로 보수적으로 생성한 한국어 역명 asset을 반영했다. 앱은 실행 중 외부 API를 호출하지 않으며, 설정에서 `일본어`, `한국어`, `함께 표시`를 선택할 수 있다.
- Yoiko 고유 역명 7,320개 전체를 자동 대조했고, 한국어 라벨이 하나로 확정된 6,738개만 asset에 넣었다. 나머지 582개는 데이터가 없거나 후보가 모호해 기존 일본어 원문을 유지한다.
- 생성기 `tool/generate_wikidata_railway_station_catalog.mjs`는 일본 철도역 카탈로그를 페이지 단위로 수집하고, `tool/generate_wikidata_station_korean_labels.mjs`는 Yoiko의 모든 고유 역명을 같은 엄격한 규칙으로 대조해 카탈로그 누락분을 보완한다. 두 생성기 모두 중단 후 재개 가능한 로컬 캐시를 사용한다.
- 설정의 역명 표기 선택은 테마 선택과 동일한 전체 폭 3분할 UI로 통일했다. `일본어`·`한국어`에는 식별 아이콘을 두고 `함께 표시`에는 아이콘을 넣지 않았다.
- 기본 InkSparkle 효과는 일부 Android 기기에서 끊겨 보일 수 있어, 앱 전체 터치 효과를 가벼운 InkRipple로 조정했다. 테마·역명 표기 세그먼트 버튼은 여기에 220ms 전환을 적용한다.
- 악성 제보 대응으로 새 제보의 익명 Auth UUID를 서버 내부에서만 연결하고, 관리자 화면에서 UUID를 노출하지 않은 채 차단·해제할 수 있게 했다. 차단된 사용자의 요청은 저장 전 거절한다. `APPLIED`·`REJECTED` 문의와 검토 이력은 해결 후 1년이 지나면 매월 Cron으로 삭제하며, 진행 중 문의는 보존한다. 원격 migration·함수 배포와 실제 Cron 실행 확인은 대기 중이다.
- 문의 관리 상세에 제보 당시 앱 파서가 확인한 승차역·하차역 snapshot을 표시한다. 역명을 확인하지 못했거나 이전 payload에 값이 없으면 서버에서 재추측하지 않고 `null`로 명시한다.

## 고정 개발 우선순위

아래 순서는 사용자가 변경을 요청하기 전까지 고정한다. 현재 단계의 완료 조건을 충족한 뒤 다음 단계로 이동한다.

| 순서 | 단계 | 현재 상태 | 구현된 내용 | 남은 완료 조건 |
|---:|---|---|---|---|
| 1 | 역 코드 데이터베이스 연결 | **완료** | 허가받은 Yoiko 데이터 8,537행 연결, `50-A5-78` 형식의 코드 조회, 승·하차역과 노선명 표시, 다중 후보 보존, 실제 역 코드 검증 | 완료. 철도 사업자 표시는 필수 범위에서 제외 |
| 2 | 거래 유형 판별 | **완료** | 실물 fixture 기반 철도 `16/01`, 버스 `05/0D`, 물품 구매 `C8/46`, 잔액 증가 충전 `C8/49` 분류와 유형별 화면 표시 | 완료. 환불·정산은 실제 샘플 확보 전까지 `UNKNOWN` 유지 |
| 3 | 금액 계산 검증 | **완료** | 이전 기록과 잔액 차이 계산, 충전 증가액 표시, 증가액·날짜 이상을 일반 사용액으로 표시하지 않는 상태 처리, 실제 금액 대조 | 완료 |
| 4 | 이용내역 로컬 저장 | **생략** | 카드 IDm을 저장하지 않는 상태에서 여러 카드 기록이 잘못 합쳐질 위험을 피하기 위해 영구 저장하지 않고 현재 스캔 세션만 표시 | 제품 범위에서 제외. 다른 카드를 스캔하면 현재 화면을 새 결과로 교체 |
| 5 | 실제 카드 검증 | **완료** | Android 실물 카드에서 NFC 읽기, 역명·거래 유형·금액·잔액 및 초기 잔액 전용 카드 표시 검증 | 완료 |
| 6 | 오류 제보 기능 연결 | **완료** | Supabase DB·Edge Function 배포, 익명 인증 Repository, 미리보기·동의·전송 성공/실패 UI, 개인정보 제외 테스트, Android 실전송·접수 내역 확인 | 완료 |
| 7 | Android 출시 준비 | **진행 중** | 앱 정보·아이콘·권한·문의 이메일·개인정보처리방침 공개, release 서명·AAB·Play Console 등록, 내부 테스트와 비공개 테스트, 오류 제보 운영 절차 완료 | 최신 AAB 비공개 배포·실기기 회귀 검증·테스트 요건 충족·프로덕션 액세스 신청 |

현재 작업 범위는 **7번 Android 출시 준비**다. 이용내역은 영구 저장하지 않고 현재 스캔 세션에서만 유지하며, 다른 카드를 스캔하면 새 결과로 교체한다. 환불·정산은 실제 샘플 확보 전까지 `UNKNOWN`으로 유지한다. iOS 개발은 Android 우선 개발과 출시 준비를 마친 뒤 Apple Developer 계정을 준비해 재개한다.

## Android 출시 준비 순서

아래 순서는 사용자가 변경을 요청하기 전까지 출시 준비의 기본 순서로 사용한다.

1. **앱 기본 정보 확정**
   - 정식 앱 이름, 패키지 이름 유지 여부, 앱 버전과 빌드 번호를 정한다.
   - 앱 아이콘과 Play Store용 간단한 소개 문구를 준비한다.
2. **개인정보 및 권한 안내 작성**
   - 개인정보처리방침을 작성하고 NFC 사용 목적을 설명한다.
   - 오류 제보 시 Supabase로 전송되는 정보와 IDm·다른 이용내역·위치를 전송하지 않는다는 점을 명시한다.
   - Yoiko 역 코드 데이터의 출처와 이용 조건을 앱과 정책 문서에 표시한다.
3. **오류 제보 운영 보완**
   - 익명 로그인 악용 방지, 무료 DB 정기 백업, 오래된 익명 Auth 사용자 정리 방식을 정한다.
   - `RECEIVED`·`REVIEWING`·`VERIFIED`·`APPLIED`·`REJECTED` 상태의 운영 절차를 정한다.
   - 네트워크 중단 시 실패 안내와 수동 다시 시도를 최종 확인한다.
4. **Android release 설정**
   - 출시용 서명 키를 만들고 Git 저장소 밖에서 안전하게 관리한다.
   - release 난독화·최적화 정책과 버전 정보를 확정한다.
   - 서명된 Android App Bundle(AAB)을 생성한다.
5. **Play Console 준비**
   - 앱을 등록하고 아이콘·스크린샷·설명·개인정보처리방침 주소를 입력한다.
   - 데이터 보안 설문, NFC 기능과 지원 기기 안내를 작성한다.
   - 내부 테스트 트랙에 AAB을 업로드한다.
6. **출시 전 최종 점검**
   - 화면 크기, 라이트·다크 모드, NFC 꺼짐·카드 모드·시간 초과를 확인한다.
   - 이용내역 없는 카드와 다수 기록 카드, 오류 제보 성공·실패를 확인한다.
   - release 빌드에서 IDm과 원시 기록이 로그에 남지 않는지 확인한다.

## 완료된 작업

- `ic_card_export/` 전체 파일 인벤토리, NFC 리더, 플랫폼 설정, DB/CSV, 조회 규칙, 테스트, 라이선스 분석.
- 원본 파일 무수정 유지.
- Flutter 앱의 feature 기반 골격과 Phase 1 domain/data/presentation 분리.
- NFC 활성 확인, 스캔 시작, 취소, 30초 시간초과, NFC-F 판별.
- 시스템 코드 `0003`, 서비스 코드 `090F`, iOS Polling, Read Without Encryption, 최대 20블록 읽기.
- Android 응답 명령/IDm echo/상태/길이 검증과 iOS 상태/길이 검증.
- IDm을 결과·로그·저장소에서 제거하고 메모리 내 통신에만 사용.
- 원시 16바이트 개발자 화면과 비FeliCa/태그 이탈/빈 이력 오류 UI.
- 순수 FeliCa 명령/응답 단위 테스트 소스 작성.
- 신고 데이터 모델/Repository 인터페이스/API·큐·운영 검토 계약 초안.
- 필수 문서 6종 초기 작성.
- Flutter 3.44.9로 Android/iOS 플랫폼 러너 생성 및 iOS entitlement 빌드 설정 연결.
- Dart format, Flutter 정적 분석, 단위/위젯 테스트, Android debug APK 빌드 성공.
- fixture 확보를 위한 debug 전용 원시 블록 터미널 로그와 개인정보 제외 포맷 테스트 추가.
- Android 실물 카드에서 읽은 IDm 없는 20블록 fixture 저장 및 형식 검증 테스트 추가.
- 실제 이동 이력 보호를 위해 실물 카드 fixture와 재배포 조건이 불명확한 `ic_card_export/`를 Git 추적 대상에서 제외.
- 16바이트 원시 이력에서 날짜·단말/처리·정규화 지역·출발/도착·잔액을 추출하는 `phase2-parser-v2` 구현.
- 일반·간이·연락 개찰 단말 `16/01`·`17/01`·`1A/01`·`1D/01`을 완전한 승하차 코드가 있을 때만 철도로 분류하고, 미검증 `23/13`은 `UNKNOWN`으로 유지.
- 자동발권기 `08/02`와 매장 단말 `C8/49`를 이전 잔액 유무와 무관하게 충전으로 분류하고, 충전 금액 계산을 일반 차감 계산과 분리.
- 마지막 원시 바이트의 상위 2비트를 지역 코드로 정규화하고 `08/02` 처리 장소를 별도 거래 장소로 보존해 자동발권기 충전역·노선 표시.
- `C8/49`는 장소를 추정하지 않고 `매장에서 충전`·`매장 단말`·`장소 확인 불가`로 보수적으로 표시.
- `phase2-parser-v3`에서 실제 `17/01` 간이 개찰 기록을 高山 → 飛騨古川·高山線으로 검증하고 회귀 fixture로 고정했으며 내부 테스트 완료.
- 물품 구매를 특정 단말 코드가 아닌 검증된 처리 코드 `46`으로 분류해 실제 `CA/46`을 지원하고, `4A`·`4B`·`C6`·`CB`는 fixture 확보 전까지 `UNKNOWN` 유지.
- `CA/46`의 바이트 6~9는 역·점포로 추정하지 않고 물품 구매로만 표시하며, 실제 인접 기록에서는 잔액 차이 `¥1,030`을 계산. 내부 테스트 완료.
- `phase2-parser-v4`에서 `1A/06`을 승차 경로가 아닌 개찰 창구 처리와 처리 장소로 분리.
- `1B/02` 모바일 충전과 `1B/48` 모바일 특전 충전을 분류하고 역명·Suica·PASMO를 원본만으로 추정하지 않도록 차단.
- `C8/C6`만 현금 병용 물품 구매로 분류하고 패킹 시각 `10:42:02`를 표시하며 다른 단말의 `C6`은 미확인 유지.
- `1A/02`를 역 개찰 단말 충전으로 분류하고 첫 코드 쌍을 天神南 처리 장소로 사용.
- 전체 시그니처가 일치하는 실제 버스 두 건에만 西日本鉄道과 名古屋市交通局 市バス를 표시.
- `phase2-parser-v5`에서 정상 `090F` 이력의 처리 코드 `02`를 충전으로 분류하고 `21/02`를 단말명을 과장하지 않은 `역 단말에서 충전`으로 표시. `AC-38`은 장소로 사용하지 않음.
- 이요테츠 高浜線 10개 역을 Yoiko 원본과 분리된 override asset으로 추가하고 실제 확인 3역과 순서 추론 7역의 근거 수준을 보존.
- 정규화 지역 `0~3`의 정확 역 코드가 없을 때 타 지역 동일 코드를 자동 확정하지 않도록 변경해 이요테츠 기록의 게이오선 오매핑을 차단.
- 실제 이요테츠 원본 두 건을 `古町 → 梅津寺`, `梅津寺 → 松山市` 및 `伊予鉄道 · 高浜線`으로 표시하는 파서·역 DB·UI 회귀 테스트 추가.
- `phase2-parser-v5.1`에서 전체 시그니처 `region=3 + 05/0D + 0F3D`에만 사업자 `伊予鉄道`을 매핑하고 교통수단은 `버스·노면전차 이용` 공통 범주로 보수적으로 표시.
- 이요테츠 차량 단말 기록에서 `市内電車`, `伊予鉄バス`, 노선·승하차 정류장을 추정하지 않으며 지역·처리·식별부가 다른 코드의 오매핑 방지 테스트 추가.
- `phase2-parser-v6.1`에서 거래 순번이 반드시 1씩 증가한다는 잘못된 금액 차단 조건을 제거. 실제 `34 → 32 → ... → 0F` 20블록 fixture에서 가장 오래된 기록을 제외한 인접 기록의 금액 계산을 복구.
- 이요테츠 郡中線 12개 역을 보완 데이터에 추가하고 `松山市`를 특정 노선 전용 역이 아닌 공통역으로 모델링.
- `03-C5` 승하차 쌍의 검증된 범위로 `郡中線`과 `高浜線`을 판정해 `郡中港 → 松山市`를 `伊予鉄道 · 郡中線`으로 표시하고, 교차 범위·미검증 조합은 노선을 확정하지 않도록 처리.
- 오류 제보 거래 유형에 `GATE_WINDOW_PROCESSING` wire 값을 추가하고 Edge Function 검증 목록을 갱신. 원격 함수 재배포 대기.
- 시스템 `애니메이션 삭제`와 무관한 30초 카운트다운 및 부드러운 진행 게이지를 내부 테스트에서 완료.
- 비공개 테스트에서 3차 수정안까지의 `1A/06`, `1B/02`, `1B/48`, `C8/C6`, `1A/02`, 제한된 버스 사업자 매핑을 실제 카드로 확인 완료.
- 비공개 테스트에서 NFC 꺼짐·카드 모드 전환·시간 초과 복귀, 오류 제보 성공·네트워크 실패·재시도, 릴리스 로그의 카드 IDm 비노출을 확인 완료.
- 인접 잔액 차이의 `CALCULATED`·`UNAVAILABLE`·`SUSPICIOUS` 상태 구현. 잔액 증가를 차감액으로 표시하지 않음.
- 스캔 성공 화면에 추출 날짜·코드·잔액·금액 상태를 표시하는 확장 카드 추가.
- 검증되지 않은 거래 유형은 `UNKNOWN`으로 보존.
- 실제 샘플이 없는 환불·정산은 분류 규칙을 만들지 않고 `UNKNOWN`으로 유지하는 회귀 테스트 추가.
- 초기 관리 기록 `08/07`·`12/07`·`22/07`은 다음 거래의 금액 계산 기준 잔액으로 사용한 뒤 화면에서만 제외하고, 단말/처리 코드 `00/00` 블록부터 읽기·파싱을 종료.
- 실물 카드와 비교 앱 결과를 근거로 `C7/46`을 물품 구매로 추가 분류.
- 스캔 중 Android NFC가 `꺼지는 중/꺼짐`으로 바뀌면 상태 스트림에서 즉시 감지하고 네이티브 `stopSession`을 다시 호출하지 않은 채 안전하게 오류 화면으로 복귀.
- Samsung 기기의 카드 전용 모드 상태값 `5`에서 `nfc_manager 4.2.1` 네이티브 수신기가 프로세스를 종료하는 원인을 확인하고, 이를 읽기 불가(`OFF`)로 전달해 스캔을 종료하고 기본 모드 변경을 안내하는 로컬 플러그인 패치 적용.
- 실물 20블록 fixture를 근거로 철도 7건, 버스 4건, 물품 구매 8건, 충전 1건을 분류하고 충전 증가액 `+¥2,000` 표시.
- 철도 기록에만 승하차역을 조회·표시하고 버스·물품 구매·충전에는 의미 없는 역 코드를 노출하지 않도록 분리.
- 버스 회사 고유 코드는 확인 전까지 추정하지 않고 목록에 `버스 회사 미확인`, 상세 화면에 `회사 고유 코드 미해석`으로 안내.
- 이용내역 아이콘을 거래 유형별 색상으로 구분하고, 상세 경로의 역명을 강조하며 충전액은 초록색으로 표시.
- 이용내역 상단에 가장 최근 거래 후 잔액을 `현재 카드 잔액`으로 표시하고 기준을 함께 안내.
- 이용내역이 없는 새 카드도 숨겨진 초기 관리 기록의 잔액을 사용해 `현재 카드 잔액`을 표시하고 `초기 잔액 기록 기준`으로 안내.
- 스캔 완료의 `확인 필요` 집계에서 가장 오래된 기록의 정상적인 `계산 불가`를 제외하고 실제 이상·미확인 기록만 계산.
- 오류 제보 3단계 전체에 선택한 이용내역 요약을 표시하고 제보 화면의 글자 굵기를 완화.
- 오류 제보 진행도를 현재 단계까지 채워지는 3분할 가로 막대와 `현재/3 단계` 문구로 표시.
- Supabase PostgreSQL migration과 RLS, 익명 사용자 JWT를 검증하는 Edge Function을 배포하고 Android에서 실제 익명 제보 접수와 Table Editor 저장을 확인.
- Android 앱과 분리된 `admin/` localhost 문의 관리 웹을 구현. 관리자 이메일·비밀번호 로그인, 상태 요약, 검색·필터, 익명 payload 상세, 관리자 메모·적용 버전·처리 상태 변경을 제공하고 브라우저에는 Publishable key만 사용.
- 관리자 전용 `issue-report-admin` Edge Function과 service role 전용 원자적 검토 RPC, `issue_report_review_logs` 감사 테이블을 추가. localhost Origin·JWT·`iccardreader10@gmail.com` 허용 목록을 모두 확인하며 배포 절차를 운영 문서에 정리했다.
- Supabase SQL에서는 문의 20건이 확인되지만 프로젝트 REST API가 한 요청에 행과 `count`를 모두 1로 잘라 관리 화면에도 1건만 표시되는 문제를 확인. 범위 조회를 폐기하고 PostgreSQL `admin_list_issue_reports` RPC가 최대 100건을 단일 JSON 값으로 집계해 반환하도록 교체하여 API 행 제한을 구조적으로 우회했으며 migration·함수 재배포 후 실데이터 확인 대기.
- 원격 migration `202608140002`와 `issue-report-admin` version 4 활성화를 CLI로 확인. 동일 GET 응답의 브라우저 재사용 가능성을 제거하도록 요청 timestamp·`cache: no-store`와 Edge 응답의 no-cache 헤더를 추가하고, 화면에 실제 로드 건수와 서버 전체 건수를 분리 표시하도록 보완.
- 진단 배지 `표시 1 / 전체 20건`으로 RPC 집계와 캐시는 정상이나 Edge Function의 누락된 `limit`이 `Number(null)=0`을 거쳐 최소값 1로 고정되는 실제 원인을 확정. null·빈 문자열은 기본값 100을 사용하도록 파라미터 변환을 분리하고 경계값 단위 테스트를 추가.
- 최신 관리자 함수 배포 후 SQL의 문의 20건이 localhost 관리 페이지에 모두 표시되는 것을 사용자 확인 완료.
- 특정 처리 상태를 선택했을 때 각 문의에 체크박스를 표시하고 현재 검색·문의 유형 조건에서 체크한 최대 100건만 다른 상태로 일괄 변경하도록 개선. 현재 필터 결과의 전체 선택·해제와 부분 선택 상태를 지원하고, 필터 변경 시 체크를 해제하며 목록은 번호·문의 유형·접수 날짜만 남겨 간소화했다. DB 트랜잭션 하나에서 선택 대상을 잠그고 변경하며 기존 메모·적용 버전을 유지하고 문의별 감사 로그를 기록.
- localhost 문의 관리 시스템은 목록 20건 표시 사용자 확인과 소스·회귀 검증을 기준으로 현재 요구 범위의 개발을 마무리했다. 일괄 변경의 원격 DB 실데이터 재검증 결과는 별도로 기록되지 않았으며, 향후 운영 재배포가 필요하면 `ISSUE_REPORT_ADMIN.md`의 migration·함수 배포 절차를 따른다.
- 제보 완료 모달을 앱의 하늘색 디자인에 맞추고 접수 번호와 IDm·다른 이용내역 미전송 안내를 표시.
- 오류 제보의 승차역·하차역·양쪽 오류와 역명 미확인을 화면의 `역 정보가 없거나 잘못됨` 유형으로 통합하고, 하단 팝업에서 승차역·하차역·둘 다 범위를 선택하게 개선. 올바른 역 이름·도시·지역은 필수, 노선명은 선택으로 받으며 거래 유형 오류는 지원 목록 선택 또는 `목록에 없음`의 직접 입력만 전송한다. 새 payload를 Edge Function·관리 페이지까지 연결하고 기존 역 오류 유형·구버전 앱의 누락 필드는 호환되게 유지했다. 원격 migration `202608140004`, `202608140005` 및 두 Edge Function 배포·실데이터 확인 대기.
- 버스 이용 기록에서만 `버스 회사 정보가 없거나 잘못됨` 유형을 노출하고, 실제 버스 회사명·운행 도시/지역을 필수로 받도록 추가했다. 노선 번호는 수집하지 않으며, Edge Function은 `BUS` 거래에만 이 payload를 허용한다. Flutter 분석·전체 97개 테스트와 관리자 Node 15개 테스트를 통과했다. 원격 migration `202608140006`과 두 Edge Function 배포·실데이터 확인 대기.
- 거래 유형 오류의 선택 UI도 드롭다운 대신 하단 팝업으로 통일했다. 철도·버스·물품 구매·충전·개찰 창구 처리·환불·정산·직접 입력을 선택할 수 있고, 직접 입력일 때만 추가 입력칸을 표시한다.
- 사용 허가를 받은 Yoiko 역 코드 8,537행을 앱 asset으로 포함하고 허가 조건 고지 등록.
- 정확 지역 코드, 안전한 지역 힌트(`50 -> 01`, `F0 -> 03`), 단일 노선·역 후보, 승하차 지역 교집합 순의 보수적 역 조회 구현.
- 다중 후보와 미등록 역은 임의 확정하지 않고 원시 코드를 유지.
- 홈 최근 기록, 이용내역 목록, 상세 화면에 확인된 역명·노선명을 연결.
- 실물 fixture 첫 기록 `50-A5-78 -> 50-AC-38`을 `名鉄名古屋 -> 中部国際空港`으로 조회하는 테스트 추가.
- 실제 역 코드 검증 완료. 철도 사업자 표시는 전체 데이터의 필수 범위는 아니지만 검증된 보완 데이터에서는 역명·노선명과 함께 제공.
- 카드에 남아 있는 가장 오래된 기록은 `계산 불가`로 유지하고, 상세 화면에서 이전 잔액 기록이 없어 금액을 계산할 수 없다는 원인을 안내.
- 하늘색 Material 3 UI, 스캔 진행/완료, 이용내역, 설정, 개발자, 신고 초안 화면 구현.
- 본 앱과 분리된 Flutter Web 교통패스 비교 프로토타입 v0.1 구현. Tokyo Subway 24/48/72시간권, 여러 수동 운임 구간, 유효시간, Metro·도에이 적용과 외부 구간 제외, 이득·본전·손해 판정을 순수 Dart 엔진으로 분리.
- 교통패스 프로토타입 v0.2에서 ODPT 토큰을 앱에 넣지 않는 개발용 정적 가져오기 도구, 정규화 역·운임 저장소, 한국어 음절·초성/일본어/영어 자동완성, 동일 사업자 실제 성인 IC 운임 자동 입력을 구현. 실제 JSON 생성은 ODPT 토큰 설정 대기.

## 진행 중인 작업

- `phase2-parser-v6.1`이 포함된 최신 AAB를 기존 비공개 테스트 `Alpha` 트랙에 배포한다.
- 최신 릴리스 설치본에서 실제 카드와 NFC 오류 흐름을 다시 검증한다.

## 다음 작업

세부 체크리스트와 완료 상태는 `docs/planning/TODO.md`를 기준으로 관리한다.

1. 최신 AAB의 Play Console 자동 검사와 검토를 완료한다.
2. 비공개 테스터의 업데이트 및 실기기 회귀 결과를 확인한다.
3. 계정에 적용되는 비공개 테스트 요건을 충족한 뒤 프로덕션 액세스를 신청한다.

## 생성·수정한 주요 파일

- `pubspec.yaml`, `README.md`, `analysis_options.yaml`
- `lib/main.dart`, `lib/src/app.dart`
- `lib/src/features/card_reader/**`
- `lib/src/features/transaction_history/**`
- `lib/src/features/station_resolver/**`
- `lib/src/features/issue_report/domain/**`
- `assets/data/stations/yoiko_station_codes.csv`
- `assets/data/stations/verified_station_overrides.csv`
- `assets/licenses/yoiko-station-data-TERMS.txt`
- `test/features/card_reader/felica_protocol_test.dart`
- `test/features/card_reader/raw_history_fixture_log_test.dart`
- `test/features/card_reader/physical_card_fixture_test.dart`
- `test/features/transaction_history/transit_history_parser_test.dart`
- `test/features/station_resolver/asset_station_database_test.dart`
- `test/fixtures/felica/android_history_20_v1.json`
- `android/app/src/main/AndroidManifest.xml`
- `ios/Runner/Runner.entitlements`, `ios/Runner/Info.plist`, `ios/Runner.xcodeproj/project.pbxproj`
- `docs/**/*.md`

## 실행한 검사

- UTF-8로 요구사항과 원자료를 읽고 파일 목록/CSV 행 수/헤더를 확인함.
- Flutter 3.44.9 / Dart 3.12.2 로컬 도구 준비 및 플랫폼 러너 생성 성공.
- `dart format lib test`: 성공.
- `flutter analyze`: 성공, 문제 0건.
- `flutter test`: 전체 94개 테스트 통과.
- `node --test admin/test/*.test.mjs`: 문의 관리 도메인·검색·상태 요약·체크한 일괄 대상 제한·날짜 간소화·limit 기본값·경계값·브라우저 비밀키 비포함·localhost 제한·단일 JSON RPC·캐시 차단·원자적 일괄 변경·감사 로그·양쪽 역 교정 계약 14개 테스트 통과.
- localhost 서버의 HTML·JavaScript 응답, CSP·no-store 보안 헤더, 로그인 화면과 일괄 변경 버튼·확인 모달 DOM 로드 및 브라우저 콘솔 오류 없음 확인.
- Android 빌드: 사용자 요청에 따라 미실행.
- iOS build: 미실행(Windows 환경).
- 실물 카드: Android에서 20블록 읽기 확인. 카드 종류·기기·OS 상세는 미기록, iOS 미검증.

## 확인되지 않은 항목

- iOS에서의 nfc_manager 런타임 동작과 실제 원시 fixture.
- Android 추가 제조사·기종에서의 카드별 Polling 및 NFC 상태 전환 안정성.
- 후속 블록 상태 플래그가 이력 종료인지 통신 오류인지에 대한 카드별 의미.
- 실제 fixture가 없는 `23/13`, `08/03`, `4A`·`4B`·`C6`·`CB`, 환불·정산 유형의 판별 규칙.
- PiTaPa 포스트페이 처리.
- Yoiko 역 데이터의 정확성 및 향후 변경 사항.

## 주요 기술 결정

- Phase 1에서는 원시 블록만 다루고 미확인 거래를 분류하지 않는다.
- IDm은 Android 명령 조립/응답 확인과 iOS 세션 내부에서만 일시 사용한다.
- Android `android.hardware.nfc`는 optional로 두고 런타임 가용성을 확인한다.
- 역 데이터 v1은 Yoiko 허가 조건 고지와 함께 번들 CSV를 한 번 로드해 메모리 인덱스로 사용한다. 데이터 단독 재배포는 금지하며 로컬 이용내역 SQLite 작업에서 사전 생성 DB 전환을 재검토한다.
- 정확 지역 조회 실패 후 단일 후보만 자동 확정하며 다중 후보는 유지한다.
- 신고는 사용자 미리보기·동의 후 한 건만 선택적으로 전송하고 자동 DB 반영하지 않는다.
- Phase 3는 디렉터리 확장 지점만 두며 Phase 2 완료 전 구현하지 않는다.
